package com.company;

// Utwórz metodę, która przyjmuje trzy parametry - boolean oraz dowolną liczbę (większą od 0) liczb (typu `int`).
// Gdy pierwszy parmametr ma wartość `true` metdoa zwraca największą przekazaną liczbą, a gdy `false` najmniejszą.
/*
public class Zadanie2 {
    public static void main(String[] args) {
        int wynik = zwrocMinLubMax(true, 6, 2, 8, 4);

    }

    private static void zwrocMinLubMax(boolean czyMax, int pierwsza, int... liczby) {
        int wynik = pierwsza;
        for (int i = 0; i < liczby.length; i++) {
            if (czyMax && liczby[i] > wynik) {
                wynik = liczby[i];
            }
            if (!czyMax && liczby[i] < wynik) {
                wynik = liczby[i];

            }
        }
        return wynik;
    }
} */
